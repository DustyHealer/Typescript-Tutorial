let arr: string[] = ['PHP', 'JS', 'TS', 'Node'];
// arr.push(123); // Gives error because string type array
arr.push('React');
console.log(arr);

let rand: [string, number, boolean] = ['1bc', 123, true]; // Only use this when array is small
let rand1 = ['1bc', 123, true, 'him', false];
